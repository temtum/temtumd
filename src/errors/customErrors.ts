import BadRequest from './customErrors/badRequest';
import NotFound from './customErrors/notFound';
import ServerError from './customErrors/serverError';

const errors = {
  BadRequest,
  NotFound,
  ServerError
};

export default errors;
